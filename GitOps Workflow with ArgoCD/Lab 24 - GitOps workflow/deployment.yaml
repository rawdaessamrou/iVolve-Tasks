apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins-app
  template:
    metadata:
      labels:
        app: jenkins-app
    spec:
      containers:
      - name: jenkins-app
        image: rawdaessamrou/jenkins-app:6
        ports:
        - containerPort: 8080
